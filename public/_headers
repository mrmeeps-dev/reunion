# For Cloudflare Pages: deploy the contents of `public/` as a static mirror, or use as a reference
# when fronting this app with Cloudflare CDN.

/styles.css
  Cache-Control: public, max-age=31536000, immutable
